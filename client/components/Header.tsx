import SearchBox from "./SearchBox";

const Header = () => (
  <header className="flex items-center justify-center min-h-[240px] md:px-10 px-4">
    <SearchBox />
  </header>
);

export default Header;
